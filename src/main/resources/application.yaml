server:
  port: 8888

spring:
  application:
    name: config-server

  cloud:
    config:
      server:
        git:
          uri: 'https://github.com/edsandrof/ms-configs.git'
          default-label: 'master'